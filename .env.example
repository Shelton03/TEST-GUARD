# Copy this file to .env and fill in your real keys.
# Never commit .env to version control.

# Your OpenAI API key (forwarded to LiteLLM / OpenAI)
OPENAI_API_KEY=sk-...

# (Optional) LLM model to use. Default: gpt-4o-mini
LLM_MODEL=gpt-4o-mini
